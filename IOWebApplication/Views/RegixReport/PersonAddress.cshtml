@using IOWebApplication.Infrastructure.Models.ViewModels.RegixReport
@model RegixPersonAddressVM

@{
    ViewData["Title"] = ViewBag.AddressTypeName;
}

@section breadcrumb{
    <ol class="breadcrumb">
        <li><a href="@Url.Action("Index","Home")" title="Начало"><i class="fa fa-home"></i></a></li>
        <li class="active">@ViewData["Title"]</li>
    </ol>
}

<div class="row">
    <div class="col-lg-12">
        <form asp-action="PersonAddress" method="post">
            @Html.HiddenFor(m => m.Report.CourtId)
            @Html.HiddenFor(m => m.AddressTypeId)
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div id="saveContainer">
                <partial name="_SaveFormContent" model="@("#saveContainer")" />
                <div class="row">
                    <div class="col-lg-2">@Html.EditorFor(m => m.PersonAddressFilter.EgnFilter)</div>
                </div>

                <partial name="_ReportReason" model="@Model.Report" />

                <div class="form-group">
                    <input type="submit" value="Търсене" class="btn btn-success" />
                </div>

                <hr />
            </div>
        </form>
        <div>
            @if (Model.Report.Id > 0)
            {
                <partial name="_PersonAddressResponse" model="@Model" />
                <a href="@Url.Action("PrintPdf","RegixReport",new { id = Model.Report.Id})" class="btn btn-primary">Свали PDF</a>
            }
        </div>
    </div>
</div>

<script>
</script>

