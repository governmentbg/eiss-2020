@using IOWebApplication.Infrastructure.Models.ViewModels.Case
@using IOWebApplication.Infrastructure.Constants
@model CaseSessionActPrintVM
@{
    Layout = null;
    string sign = "_______________________";
}
<div style="float:right;width:100%;margin-top:30px;clear:right;">
    <table class="no-border row-space-single" style="float:right;clear:left;">
        @if ((Model.JudgeList.Count == 0 && Model.JuryList.Count == 0) || (string.IsNullOrEmpty(Model.JudgeChairman) && Model.JudgeList.Count == 1))
        {
            @if (Model.GenerateExecProcess && NomenclatureConstants.ActType.ExecListActs.Contains(Model.ActTypeId))
            {
                <tr>
                    <td align="right">
                        <b>Изпълнителният лист е подписан с квалифициран електронен подпис от съдия:</b>
                    </td>
                </tr>
                <tr>
                    <td align="right">
                        &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;( @Model.JudgeReporter )
                    </td>
                </tr>
            }
            else
            {
                <tr>
                    <td align="right">
                        <b>Съдия при @Model.CourtName:</b>
                    </td>
                    <td>@sign</td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td align="right">
                    <b>Председател:</b>
                </td>
                <td>@sign</td>
            </tr>
            @if ((Model.AllJudgeList.Count > 0) && (!Model.ChairmanSignOnly))
            {
                <tr>
                    <td align="right">
                        <b>
                            @if (Model.AllJudgeList.Count == 1)
                            {
                                <text>Член-съдия:</text>
                            }
                            else
                            {
                                <text>Членове:</text>
                            }
                        </b>
                    </td>
                    <td></td>
                </tr>
                int i = 0;
                foreach (var item in Model.AllJudgeList)
                {
                    i++;
                    <tr>
                        <td align="right">
                            @(i).
                        </td>
                        <td>@sign</td>
                    </tr>
                }
            }
            @if (Model.JuryList.Count > 0 && NomenclatureConstants.ActType.JurySign.Contains(Model.ActTypeId))
            {
                <tr>
                    <td align="right">
                        <b>Заседатели:</b>
                    </td>
                    <td></td>
                </tr>
                int i = 0;
                foreach (var item in Model.JuryList)
                {
                    i++;
                    <tr>
                        <td align="right">
                            @(i).
                        </td>
                        <td>@sign</td>
                    </tr>
                }
            }
        }
        @if (Model.ActFormatType == NomenclatureConstants.ActFormatType.Protokol)
        {
            @if (!string.IsNullOrEmpty(Model.SecretaryName) || Model.SecretaryList.Count == 1)
            {
                <tr>
                    <td align="right">
                        <b>Секретар:</b>
                    </td>
                    <td>@sign</td>
                </tr>
            }
            @if (Model.SecretaryList.Count > 1)
            {
                <tr>
                    <td align="right">
                        <b>Секретари:</b>
                    </td>
                    <td></td>
                </tr>
                int i = 0;
                foreach (var item in Model.SecretaryList)
                {
                    i++;
                    <tr>
                        <td align="right">
                            @(i).
                        </td>
                        <td>@sign</td>
                    </tr>
                }
            }
        }
    </table>
</div>
