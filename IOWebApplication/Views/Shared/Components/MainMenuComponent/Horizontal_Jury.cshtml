@model string
<nav class="navbar navbar-static-top">
    <div class="container menu-container">
        <div class="navbar-header">
            <a asp-action="Index" asp-controller="Home" class="navbar-brand"><i class="fa fa-balance-scale"></i>&nbsp;<b>ЕИСС</b></a>
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
                <i class="fa fa-bars"></i>
            </button>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse pull-left" id="navbar-collapse">
            <ul class="nav navbar-nav">

                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-balance-scale"></i> Съдебни дела<span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a href="@Url.Action("IndexForLawUnitCurrent", "CaseSessionAct")"><i class="fa fa-book"></i> Съдебни актове и протоколи</a></li>
                    </ul>
                </li>

            </ul>
        </div>

        @await Component.InvokeAsync(nameof(IOWebApplication.Components.MainMenuComponent), new { rightBar = true })
    </div>
    <!-- /.container-fluid -->
</nav>
