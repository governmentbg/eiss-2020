@using IOWebApplication.Infrastructure.Models.ViewModels.Common.Mediation
@model MediationCenterVM

@{
    if (Model.Id > 0)
    {
        ViewData["Title"] = "Редакция на център за медиация";
    }
    else
    {
        ViewData["Title"] = "Добавяне на център за медиация";
    }

    string d = ViewBag.desc;
}

@section breadcrumb {
    <ol class="breadcrumb">
        <li><a href="@Url.Action("Index", "Home")" title="Начало"><i class="fa fa-home"></i></a></li>
        <li><a href="@Url.Action("IndexMediationCenter", "MediationCommon")">Центрове за медиация</a></li>
        <li class="active">@ViewData["Title"]</li>
    </ol>
}

<div class="row">
    <div class="col-md-6">
        <form asp-action="EditMediationCenter" method="post">
            @Html.HiddenFor(x => x.Id)

            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div id="saveContainer">
                <partial name="_SaveFormContent" model="@("#saveContainer")" />

                <div class="row">
                    <div class="col-lg-6">@Html.EditorFor(x => x.Name)</div>
                    <div class="col-lg-6">@Html.EditorFor(x => x.ContactDetails)</div>
                </div>

                <div class="row">
                    <div class="col-lg-12">@Html.EditorFor(x => x.AddressText)</div>
                </div>

                <div class="row">
                    <div class="col-lg-4">@Html.EditorFor(x => x.DateFrom, "DateTime")</div>
                    <div class="col-lg-4">@Html.EditorFor(x => x.DateTo, "DateTime")</div>
                </div>

                @* <div class="row">
                    <div class="col-lg-12">@Html.EditorFor(x => x.ParentId, "GenericSelect2")</div>
                </div> *@

                <div class="row">
                    <div class="col-lg-12">@Html.EditorFor(x => x.Description, "Textarea")</div>
                </div>

                <div class="row">
                    <div class="col-lg-12">@Html.EditorFor(x => x.CourtIds, "GenericDropDownMultipleArray")</div>
                </div>
            </div>

            <div class="form-group">
                <input type="submit" value="Запис" class="btn btn-success single-click-submit" />
                <a asp-controller="MediationCommon" asp-action="IndexMediationCenter" class="btn btn-danger">Назад</a>
                @if (Model.Id > 0)
                {
                    <div class="pull-right">
                        @await Html.PartialAsync("_ShowLogOperation", new IOWebApplication.Core.Models.ShowLogModel() { ObjectKey = Model.Id.ToString() })
                    </div>
                }
            </div>
        </form>
    </div>
</div>

@section scripts {
    <script>
        $(document).ready(function () {
            $('#ParentId').select2({ language: "bg" });
            $('#CourtIds').select2({ language: "bg" });

            cStr = '@Model.StringCourtIds';
            var cSelectedValues = cStr.split(",");
            $('#CourtIds').val(cSelectedValues).trigger('change');
        });
    </script>
}
